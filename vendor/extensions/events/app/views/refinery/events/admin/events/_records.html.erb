<%= render 'refinery/admin/search_header', :url => refinery.events_admin_events_path %>
<div class='pagination_container'>
  <% if @events.any? %>
    <%= render 'events' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
